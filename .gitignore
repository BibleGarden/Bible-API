.env
__pycache__/
*.pyc
.venv/
# Default host directory of the embedding weights (docker-compose.yml,
# EMBEDDING_MODELS_DIR): gigabytes of model files, never in git.
models/
# The evaluation suite now lives in BibleGarden/AI-Evaluation. Keep legacy
# local caches in place during the migration without making them candidates
# for this repository.
evaluation/
